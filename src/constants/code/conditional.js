export default
`var numeroUno = 3;
var numeroDos = 5;

if (numeroUno > numeroDos) {
    console.log(numeroUno + " is bigger than " + numeroDos + "!");
} else if (numeroUno < numeroDos) {
    console.log(numeroDos + " is bigger than " + numeroUno + "!");
} else {
    console.log(numeroUno + " and " + numeroDos + " are the same!");
}`